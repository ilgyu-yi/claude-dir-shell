- Add the `dir` CLI and standalone workspace scaffolder (`dir init-workspace`, `dir file`) that make Initiative filing runnable end-to-end, writing a filing record (standalone) or creating an Active `initiative` Issue (repo mode). (#11)
